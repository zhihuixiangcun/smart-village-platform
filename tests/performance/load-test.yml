config:
  target: 'http://localhost:3001'
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
    - get:
        url: "/api/v1/health"
    - get:
        url: "/api/v1/dashboard"
    - think: 1
