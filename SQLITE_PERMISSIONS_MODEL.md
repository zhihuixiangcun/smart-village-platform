# SQLite权限模型设计文档

## 概述

本文档描述了基于MongoDB模型设计的增强型SQLite权限模型。该模型实现了多层权限控制、角色继承、审计日志和数据敏感性控制。

## 数据库表结构

### 1. 权限模块表 (permission_modules)
存储系统中的功能模块信息。

### 2. 权限操作表 (permission_actions)
存储系统中允许的操作类型。

### 3. 权限定义表 (permission_definitions)
定义每个模块-操作对的详细信息，包括风险级别和审批要求。

### 4. 用户角色表 (user_roles)
存储系统中的角色信息，包括层次结构。

### 5. 角色权限表 (role_permissions)
映射角色到具体的权限。

### 6. 用户角色分配表 (user_role_assignments)
为用户分配角色。

### 7. 权限审计日志表 (permission_audit_logs)
记录权限相关操作的审计日志。

## 权限层次结构

1. 系统管理员 (system_admin) - 最高权限
2. 村庄管理员 (village_admin) - 村庄管理权限
3. 委员会成员 (committee_member) - 基本权限
4. 居民用户 (resident) - 最低权限

## 风险控制

- 低风险操作：直接执行
- 中等风险操作：记录日志
- 高风险操作：需要审批
- 关键风险操作：需要多级审批

## 实现特性

1. 基于角色的访问控制 (RBAC)
2. 个人权限覆盖
3. 职位模板权限
4. 权限继承
5. 审计跟踪
6. 数据敏感性控制