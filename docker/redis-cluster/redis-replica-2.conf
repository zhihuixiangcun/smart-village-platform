# Redis Replica Node 2 Configuration (Replica of Master 2)
# Smart Village Platform - Redis Cluster Configuration

port 7005
bind 0.0.0.0
protected-mode yes
daemonize no
pidfile /var/run/redis-7005.pid
loglevel notice
logfile "/var/log/redis/redis-7005.log"
databases 16
maxmemory 4gb
maxmemory-policy allkeys-lru
appendonly yes
appendfilename "appendonly-replica-2.aof"
dbfilename dump-replica-2.rdb
dir /data

# === Cluster Configuration ===
cluster-enabled yes
cluster-config-file nodes-replica-2.conf
cluster-node-timeout 5000
cluster-replica-validity-factor 10
cluster-migration-barrier 1
cluster-require-full-coverage yes

# === Performance ===
slowlog-log-slower-than 10000
slowlog-max-len 128
latency-monitor-threshold 100
hz 10
dynamic-hz yes
