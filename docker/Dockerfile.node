# Node.js 服务 Dockerfile
FROM node:20-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm ci --only=production

# 复制源代码
COPY src/ ./src/
COPY uploads/ ./uploads/

# 创建日志目录
RUN mkdir -p logs

# 暴露端口
EXPOSE 3001

# 启动服务
CMD ["node", "src/app.js"]
