# 智慧村庄综合服务平台 - 项目级开发规则文档

本文档规定了智慧村庄平台开发过程中必须遵循的核心规则，确保代码质量、功能完整性和系统稳定性。

---

## 1. 纠错行为规范 - 核心保护规则

### 1.1 激活条件
当 AI 工具应用户要求执行以下任意行为时，即视为进入"纠错风险态"，**必须激活本规则并执行后续流程**：

- 修改已有模块以解决运行错误、类型错误、编译报错等问题
- 删除、注释、简化原有核心逻辑，如功能判断、跳转、表单校验等
- 使用默认返回值(如 `return true/null`)临时绕过逻辑
- 更改函数的结构性路径、状态机流程、依赖注入、接口调用等关键链路
- 修改数据库模型、API 接口结构、组件 props 定义等

### 1.2 AI 工具角色定义
AI 工具在本规则中角色为：**辅助执行者**，不具备独立结构性决策权。

仅可在满足以下任意前提之一的条件下继续执行修复行为：
- 明确读取到模块意图文档，确认修改不会破坏既有结构
- 修改范围仅限于语法错误、拼写错误、依赖路径等非逻辑性问题
- 用户明确授权进行结构性修改

### 1.3 强制文档记录机制
任何结构性修改都必须在执行前创建修改记录，包含：

```markdown
### 修改记录 - [日期时间]
- **修改模块**: 文件路径:行号
- **修改内容摘要**: 修改后代码行为简述
- **修改原因**: 错误类型及定位依据
- **风险评估**: 可能影响的其他模块或功能
- **建议恢复方式**: 如问题解决后应如何还原逻辑
- **测试验证**: 需要验证的功能点
```

### 1.4 用户确认机制
若修改涉及以下结构性操作，必须中止并请求用户确认：

- 删除或注释整段逻辑，如函数体、流程判断分支
- 使用 `return true/null/undefined` 等值绕过流程
- 绕过鉴权、跳转、Token 存储、表单校验等关键逻辑
- 更改状态管理、错误捕获结构、跳转路径等控制性逻辑
- 修改数据库 Schema、API 路由结构、组件接口定义

---

## 2. 模块保护规则

### 2.1 核心模块清单
以下模块被定义为**核心保护模块**，修改时必须格外谨慎：

#### 后端核心模块
- `server/models/` - 数据库模型定义
- `server/routes/` - API 路由和业务逻辑
- `src/middleware/` - 中间件和安全组件
- `src/services/` - 核心业务服务
- `server/app.js` - 主服务器配置

#### 前端核心模块
- `client/src/api/` - API 接口定义
- `client/src/store/` - 状态管理
- `client/src/router/` - 路由配置
- `client/src/components/` - 核心组件
- `client/src/views/` - 页面组件

#### 配置文件
- `package.json` - 依赖管理
- `.env` 文件 - 环境配置
- `jest.config.js` - 测试配置

### 2.2 功能完整性保护
保护以下核心功能的完整性：

- **认证授权系统**: 登录、权限验证、Token 管理
- **数据库操作**: CRUD 操作、事务处理、数据验证
- **文件上传**: 文件处理、路径管理、安全验证
- **实时通信**: Socket.IO 连接、事件处理
- **国际化系统**: 多语言支持、文化适配
- **监控系统**: 性能监控、错误日志、健康检查

---

## 3. 代码修改标准

### 3.1 安全第一原则
- 禁止在生产代码中硬编码敏感信息
- 所有用户输入必须进行验证和过滤
- 文件上传必须包含类型和大小验证
- 数据库操作必须防范 SQL 注入

### 3.2 向后兼容原则
- API 接口修改必须保持向后兼容
- 数据库 Schema 变更需要提供迁移脚本
- 组件 props 变更需要保持默认值兼容

### 3.3 错误处理标准
- 所有异步操作必须包含错误处理
- 用户友好的错误提示信息
- 详细的服务器端错误日志
- 优雅的错误恢复机制

---

## 4. 测试验证规则

### 4.1 修改后必须验证的功能
- 用户登录和权限验证
- 数据 CRUD 操作
- 文件上传和下载
- 实时通信功能
- API 接口响应格式
- 前端页面渲染和交互

### 4.2 测试执行顺序
```bash
# 1. 语法检查
npm run lint

# 2. 类型检查（如适用）
npm run typecheck

# 3. 单元测试
npm test

# 4. 集成测试
npm run test:integration

# 5. 端到端测试
npm run test:e2e
```

---

## 5. 文档维护规则

### 5.1 强制文档更新场景
- API 接口变更必须更新 API 文档
- 数据库模型变更必须更新 Schema 文档
- 新功能开发必须更新用户手册
- 配置变更必须更新部署文档

### 5.2 文档结构要求
```
docs/
├── api/           # API 接口文档
├── database/      # 数据库设计文档
├── deployment/    # 部署配置文档
├── user-guide/    # 用户使用手册
└── development/   # 开发指南
```

---

## 6. 紧急修复流程

### 6.1 生产环境紧急修复
1. **立即评估**: 确定影响范围和严重程度
2. **最小修改**: 仅修改必要的代码，避免大范围变更
3. **快速验证**: 在测试环境验证修复效果
4. **记录文档**: 详细记录修复过程和影响
5. **后续优化**: 计划更完善的长期解决方案

### 6.2 回滚准备
- 每次修改前备份相关文件
- 保留原有逻辑的注释版本
- 准备快速回滚脚本
- 监控修改后的系统表现

---

## 7. 代码审查要求

### 7.1 自动化检查
- 代码格式化检查
- 语法和类型检查
- 安全漏洞扫描
- 测试覆盖率检查

### 7.2 人工审查重点
- 业务逻辑正确性
- 安全性考量
- 性能影响评估
- 代码可维护性

---

## 8. 违规处理

### 8.1 违规行为定义
- 未经确认进行结构性修改
- 绕过安全验证逻辑
- 删除核心功能代码
- 未记录重要修改

### 8.2 处理流程
1. 立即停止当前操作
2. 评估已造成的影响
3. 如有必要，回滚到安全状态
4. 重新规划修改方案
5. 按规则重新执行

---

## 9. 规则更新机制

本规则文档会根据项目发展需要进行更新，所有修改都会：
- 记录变更历史
- 通知相关开发人员
- 提供迁移指南
- 更新相关工具配置

---

**最后更新**: 2025-09-21
**版本**: v1.0
**适用范围**: 智慧村庄综合服务平台全栈开发

---

*本规则文档具有强制执行力，所有参与项目开发的 AI 工具和开发人员都必须严格遵循。*